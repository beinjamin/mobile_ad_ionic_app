import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mobile_ad_ionic_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
